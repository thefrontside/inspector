{
  // checks cover all .ts files,
  // but excludes the ui directory which has its own tsconfig
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "."
  },
  "include": ["**/*.ts", "package.json"],
  "exclude": ["dist", "ui"]
}
